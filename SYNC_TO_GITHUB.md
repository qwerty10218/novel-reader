# GitHub 同步規則

本文件規範《彼岸仍是人間》線上閱讀器倉庫的同步流程。此規則用來避免上下文壓縮後遺失操作習慣。

## 核心原則

- 每次同步前，先在 `web` 目錄檢查本地狀態與變更摘要。
- 本地進度應先整理成 commit，保存為可回溯節點。
- 本地 commit 不等於必然 push。
- commit 後必須判斷哪些內容適合上傳 GitHub repo。
- 不適合上傳的內容，保留在本地並回報原因。

## 標準流程

1. 執行 `git status --short --branch` 確認目前分支與變更。
2. 執行 `git fetch origin` 確認遠端狀態。
3. 執行 `git diff --stat` 與 `git diff --name-status` 檢查變更範圍。
4. 對閱讀器腳本執行必要檢查，例如 `node --check js/script.js`。
5. 使用 `git add .` 與 `git commit -m "..."` 先建立本地 commit。
6. commit 後審核內容是否適合推送。
7. 通過審核才執行 `git push origin main`。

## 可直接推送的內容

- 已定稿或已校準的 `chapters/*.md`。
- 正式閱讀器功能檔案，例如 `index.html`、`css/`、`js/`。
- 正式閱讀器資產，例如已確認可公開的 `assets/` 圖片。
- 與閱讀器運作直接相關的文件，例如 `README.md` 或本同步規則。

## 暫不自動推送的內容

- 未定稿草稿、臨時筆記、測試輸出。
- 含有私密資訊、帳號、token、API key、個人資料的檔案。
- 大型中間檔、暫存檔、截圖測試殘留、工具生成快取。
- 尚未確認是否應公開的創作備忘、內部推演、敏感設定。
- 可能破壞章節年代線、正體字規範或閱讀器功能的變更。

## 回報格式

同步完成後，回報：

- 本地 commit hash 與訊息。
- 是否已 push 到 GitHub。
- 若未 push，說明保留本地的原因。
- 若有風險或待確認內容，列出具體檔案。
